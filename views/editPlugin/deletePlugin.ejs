<div class="space-y-3">
    <h2 class="doc-card-title">Delete plugin</h2>
    <p class="text-sm leading-relaxed text-slate-600">
        This permanently removes the plugin and all associated metrics. Type the plugin name to confirm.
    </p>
    <div class="input-group">
        <label class="input-label" for="confirm_plugin_name">Plugin name</label>
        <input id="confirm_plugin_name" type="text" class="input-control" placeholder="<%= plugin.name %>">
        <p class="form-helper">Enter “<%= plugin.name %>” to enable the delete button.</p>
    </div>
    <button id="delete_button" type="button" class="btn-primary w-full justify-center opacity-50 cursor-not-allowed" disabled>Delete plugin</button>
</div>
