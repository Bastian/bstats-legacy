<div class="space-y-6">
    <div class="space-y-3">
        <h2 class="doc-card-title">Add custom chart</h2>
        <p class="text-sm leading-relaxed text-slate-600">
            Select a chart type and configure the options. Once created, the chart appears immediately on your plugin page.
        </p>
    </div>

    <div class="input-group">
        <label class="input-label" for="select_chart">Chart type</label>
        <select id="select_chart" class="input-control">
            <option value="" disabled selected>Select a chart</option>
            <optgroup label="Pie charts">
                <option value="simple_pie">Simple Pie</option>
                <option value="advanced_pie">Advanced Pie</option>
                <option value="drilldown_pie">Drilldown Pie</option>
            </optgroup>
            <optgroup label="Line charts">
                <option value="single_linechart">Single Line Chart</option>
            </optgroup>
            <optgroup label="Bar charts">
                <option value="simple_bar" disabled>Simple Bar Chart (coming soon)</option>
                <option value="advanced_bar" disabled>Advanced Bar Chart (coming soon)</option>
            </optgroup>
            <optgroup label="Map">
                <option value="mapchart" disabled>Map Chart (not available)</option>
            </optgroup>
        </select>
    </div>

    <div id="chartForms" class="space-y-6">
        <% include forms/simplePieForm.ejs %>
        <% include forms/advancedPieForm.ejs %>
        <% include forms/drilldownPieForm.ejs %>
        <% include forms/singleLineChartForm.ejs %>

        <div data-chart-form="simple_bar" class="hidden rounded-2xl border border-slate-200 bg-slate-50 px-4 py-5 text-sm text-slate-500">
            Bar charts will return soon. We’re giving them a design refresh.
        </div>
        <div data-chart-form="advanced_bar" class="hidden rounded-2xl border border-slate-200 bg-slate-50 px-4 py-5 text-sm text-slate-500">
            Advanced bar charts are currently disabled while we rework the editor.
        </div>
        <div data-chart-form="mapchart" class="hidden rounded-2xl border border-slate-200 bg-slate-50 px-4 py-5 text-sm text-slate-500">
            Map charts are not available right now. Let us know if you’d like to see them return.
        </div>
    </div>
</div>
